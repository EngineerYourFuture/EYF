/**
 * Re-export of the canonical Field primitive. Kept so the content pages'
 * existing `../_field` imports keep working; the implementation now lives in
 * @eyf/ui so the labelled-field pattern is defined once.
 */
export { Field } from "@eyf/ui";
